# Core ML
torch>=2.1.0
torchvision>=0.16.0
transformers>=4.40.0
datasets>=2.14.0

# Vision
Pillow>=10.0.0

# Analysis & visualisation
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
scikit-learn>=1.3.0
tqdm>=4.65.0

# Config
PyYAML>=6.0

# COCO evaluation (for Karpathy evaluation pipeline)
pycocotools>=2.0.7
pycocoevalcap>=1.2

# Optional: required for Stage 3 distributed training
# flash-attn>=2.3.0  # Install separately: pip install flash-attn --no-build-isolation
# bitsandbytes>=0.41.0  # Required for 8-bit quantisation in Stage 2
